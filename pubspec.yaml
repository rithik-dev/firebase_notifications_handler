name: firebase_notifications_handler
description: Flutter package for easily setting up firebase push notifications.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^1.3.0
  firebase_messaging: ^10.0.2
  flutter_local_notifications: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
